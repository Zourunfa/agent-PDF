# 立即修复 - 数据库迁移问题

## 🔴 问题

你看到这个错误：
```
Could not find a relationship between 'user_pdfs' and 'pdf_conversations'
```

**原因**: 数据库表还没有创建

## ✅ 快速修复 (2 分钟)

### 步骤 1: 打开 Supabase 仪表板

访问: https://app.supabase.com

### 步骤 2: 选择你的项目

点击你的项目名称

### 步骤 3: 打开 SQL Editor

左侧菜单 → SQL Editor

### 步骤 4: 创建新查询

点击 "New Query"

### 步骤 5: 复制并运行以下 SQL

```sql
-- 创建 pdf_conversations 表
CREATE TABLE IF NOT EXISTS pdf_conversations (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  pdf_id UUID REFERENCES user_pdfs(id) ON DELETE CASCADE NOT NULL,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  message_count INTEGER DEFAULT 0,
  last_message_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(pdf_id, user_id)
);

-- 创建索引
CREATE INDEX IF NOT EXISTS idx_pdf_conversations_user_id ON pdf_conversations(user_id);
CREATE INDEX IF NOT EXISTS idx_pdf_conversations_pdf_id ON pdf_conversations(pdf_id);

-- 创建 conversation_messages 表
CREATE TABLE IF NOT EXISTS conversation_messages (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  conversation_id UUID REFERENCES pdf_conversations(id) ON DELETE CASCADE NOT NULL,
  pdf_id UUID REFERENCES user_pdfs(id) ON DELETE CASCADE NOT NULL,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  role TEXT NOT NULL CHECK (role IN ('user', 'assistant')),
  content TEXT NOT NULL,
  tokens INTEGER,
  processing_time INTEGER,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 创建索引
CREATE INDEX IF NOT EXISTS idx_conversation_messages_conversation_id ON conversation_messages(conversation_id);
CREATE INDEX IF NOT EXISTS idx_conversation_messages_pdf_id ON conversation_messages(pdf_id);
CREATE INDEX IF NOT EXISTS idx_conversation_messages_user_id ON conversation_messages(user_id);

-- 启用 RLS
ALTER TABLE pdf_conversations ENABLE ROW LEVEL SECURITY;
ALTER TABLE conversation_messages ENABLE ROW LEVEL SECURITY;

-- 创建 RLS 策略
CREATE POLICY "Users can view own conversations" ON pdf_conversations FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can insert own conversations" ON pdf_conversations FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can view own messages" ON conversation_messages FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can insert own messages" ON conversation_messages FOR INSERT WITH CHECK (auth.uid() = user_id);

-- 创建触发器函数
CREATE OR REPLACE FUNCTION update_conversation_stats()
RETURNS TRIGGER AS $$
BEGIN
  UPDATE pdf_conversations
  SET message_count = message_count + 1, last_message_at = NOW(), updated_at = NOW()
  WHERE id = NEW.conversation_id;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- 创建触发器
DROP TRIGGER IF EXISTS trigger_update_conversation_stats ON conversation_messages;
CREATE TRIGGER trigger_update_conversation_stats
AFTER INSERT ON conversation_messages
FOR EACH ROW
EXECUTE FUNCTION update_conversation_stats();
```

### 步骤 6: 点击 "Run"

等待 SQL 执行完成，应该看到 "Success" 消息

### 步骤 7: 刷新应用

1. 回到应用
2. 刷新页面 (Ctrl+R 或 Cmd+R)
3. 打开浏览器开发者工具 (F12)
4. 查看 Network 标签页

### 步骤 8: 验证成功

应该看到：
- ✅ `/api/pdfs/list` 请求返回 200
- ✅ 不再看到 500 错误
- ✅ 左侧显示 PDF 列表

## 🎉 完成！

现在你可以：
1. ✅ 上传 PDF
2. ✅ 进行对话
3. ✅ 刷新页面查看 PDF 列表
4. ✅ 点开 PDF 查看对话历史
5. ✅ 继续对话

---

**就这么简单！** 🚀

如果还有问题，查看 `数据库迁移修复指南.md`
